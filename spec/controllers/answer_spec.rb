require "spec_helper"

describe Answer do
  context 'Answer validations' do
    # it 'Should not save an answer if the body is empty' do
    #   # expect((Answer.new).valid?).to be(true)
    # end

    # it 'Should not save a question with an empty title' do
    #     answer = Answer.new(body: nil)
    #     expect(answer.valid?).to eq(false)
    # end
  end

	# if "cannot be submitted if a user is not signed in"

	# end
end
